# 树状数组与前缀和：代价与适用边界

**日期**：2026-08-28　**领域**：算法　**编号**：27/9000

## 背景

任何方案都有不适用的时候，提前写清楚。这里围绕「树状数组与前缀和」做一次整理，重点放在能直接落地的部分。

## 要点

- 单点更新 + 区间查询，lowbit 定位父节点
- 区间和需用两个前缀和相减
- 把不适用条件写进文档，避免误用。

## 示例

```python
def add(i, v):
    while i <= n:
        bit[i] += v
        i += i & -i
```

## 易错点

- 下标从 1 开始，原数组需要整体偏移

## 小结

树状数组与前缀和 在代价与适用边界这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
