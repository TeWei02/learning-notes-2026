# 拓扑排序与入度表：基础用法与最小示例

**日期**：2026-08-28　**领域**：算法　**编号**：51/9000

## 背景

先写出能跑通的最小示例，把输入输出钉死，再谈扩展。这里围绕「拓扑排序与入度表」做一次整理，重点放在能直接落地的部分。

## 要点

- Kahn 算法逐层剥入度为零的点，天然给出分层信息
- 若结果长度小于节点数说明有环
- 最小示例留着，回归时可直接复用。

## 示例

```python
indeg = [0] * n
for u, v in edges:
    indeg[v] += 1
q = deque(i for i in range(n) if indeg[i] == 0)
```

## 易错点

- 有环时不要无限循环，需提前判断

## 小结

拓扑排序与入度表 在基础用法与最小示例这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
