# SQL 注入与参数化查询：与相邻技术的配合

**日期**：2026-08-28　**领域**：安全　**编号**：24/9000

## 背景

它很少单独出现，和上下游的约定决定了整体效果。这里围绕「SQL 注入与参数化查询」做一次整理，重点放在能直接落地的部分。

## 要点

- 拼接字符串是注入根源，参数化让数据与语句分离
- 表名与列名无法参数化，需要白名单校验
- 跨组件问题先在边界处抓包或打日志。

## 示例

```python
cur.execute('SELECT * FROM users WHERE email = %s', (email,))
```

## 易错点

- ORM 中的原生 SQL 拼接同样有风险

## 小结

SQL 注入与参数化查询 在与相邻技术的配合这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
