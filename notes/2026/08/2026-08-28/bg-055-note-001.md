# 状态管理的单一数据源：基础用法与最小示例

**日期**：2026-08-28　**领域**：Web　**编号**：55/9000

## 背景

先写出能跑通的最小示例，把输入输出钉死，再谈扩展。这里围绕「状态管理的单一数据源」做一次整理，重点放在能直接落地的部分。

## 要点

- 派生状态不要重复存储，否则需要同步逻辑
- 异步状态用显式的加载与错误枚举表示
- 最小示例留着，回归时可直接复用。

## 示例

```js
const rows = computed(() => items.filter(i => i.ok))
```

## 易错点

- 把加载状态散落各处会导致界面闪烁

## 小结

状态管理的单一数据源 在基础用法与最小示例这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
