# Trie 的空间与查询权衡：代码评审关注点

**日期**：2026-08-28　**领域**：算法　**编号**：43/9000

## 背景

评审优先看契约与失败路径，其次才是实现技巧。这里围绕「Trie 的空间与查询权衡」做一次整理，重点放在能直接落地的部分。

## 要点

- 用字典嵌套实现简单但内存开销大，数组实现更紧凑
- 前缀查询与自动补全共用同一结构
- 把重复出现的意见沉淀成团队约定。

## 示例

```python
def insert(root, word):
    node = root
    for ch in word:
        node = node.setdefault(ch, {})
    node['#'] = True
```

## 易错点

- 计数场景要在节点上保存额外字段

## 小结

Trie 的空间与查询权衡 在代码评审关注点这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
