# 时序数据的压缩与保留：基础用法与最小示例

**日期**：2026-08-28　**领域**：数据　**编号**：57/9000

## 背景

先写出能跑通的最小示例，把输入输出钉死，再谈扩展。这里围绕「时序数据的压缩与保留」做一次整理，重点放在能直接落地的部分。

## 要点

- 按时间分区并结合保留策略，冷热分离
- 预聚合能极大降低查询扫描量
- 最小示例留着，回归时可直接复用。

## 示例

```python
CREATE TABLE m_1h AS SELECT date_trunc('hour', ts) h, avg(v) FROM raw GROUP BY 1;
```

## 易错点

- 原始精度数据全量长期保留成本很高

## 小结

时序数据的压缩与保留 在基础用法与最小示例这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
