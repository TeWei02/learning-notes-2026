# 密码存储与哈希选择：配置与参数调优

**日期**：2026-08-28　**领域**：安全　**编号**：16/9000

## 背景

参数调整必须一次只改一个，否则无法归因。这里围绕「密码存储与哈希选择」做一次整理，重点放在能直接落地的部分。

## 要点

- 使用专门的口令哈希，如 bcrypt 或 argon2，务必加盐
- 迭代成本要可调，以便随硬件升级提高
- 调优结论要绑定当时的负载特征。

## 示例

```python
from argon2 import PasswordHasher
ph = PasswordHasher()
ph.hash(pw)
```

## 易错点

- SHA-256 加盐仍可被 GPU 高速爆破

## 小结

密码存储与哈希选择 在配置与参数调优这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
