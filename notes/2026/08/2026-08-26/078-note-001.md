# 密码存储与哈希选择：版本迁移与兼容处理

**日期**：2026-08-26　**领域**：安全　**编号**：78/5000

## 背景

升级前先确认依赖链上的行为差异。这里围绕「密码存储与哈希选择」做一次整理，重点放在能直接落地的部分。

## 要点

- 使用专门的口令哈希，如 bcrypt 或 argon2，务必加盐
- 迭代成本要可调，以便随硬件升级提高
- 灰度期间保留回退路径。

## 示例

```python
from argon2 import PasswordHasher
ph = PasswordHasher()
ph.hash(pw)
```

## 易错点

- SHA-256 加盐仍可被 GPU 高速爆破

## 小结

密码存储与哈希选择 在版本迁移与兼容处理这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
