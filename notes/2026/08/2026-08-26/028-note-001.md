# 时序数据的压缩与保留：性能与复杂度权衡

**日期**：2026-08-26　**领域**：数据　**编号**：28/5000

## 背景

先量化再优化，凭感觉换实现经常得不偿失。这里围绕「时序数据的压缩与保留」做一次整理，重点放在能直接落地的部分。

## 要点

- 按时间分区并结合保留策略，冷热分离
- 预聚合能极大降低查询扫描量
- 优化前后都要有可复现的基准数据。

## 示例

```python
CREATE TABLE m_1h AS SELECT date_trunc('hour', ts) h, avg(v) FROM raw GROUP BY 1;
```

## 易错点

- 原始精度数据全量长期保留成本很高

## 小结

时序数据的压缩与保留 在性能与复杂度权衡这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
