# CDC 与增量同步的位点管理：方案对比与选型

**日期**：2026-08-26　**领域**：数据库　**编号**：49/5000

## 背景

把候选方案的代价列清楚，选型就不再靠偏好。这里围绕「CDC 与增量同步的位点管理」做一次整理，重点放在能直接落地的部分。

## 要点

- 位点必须与数据变更同事务提交，否则会丢事件或重复
- 至少一次投递需要下游幂等
- 选型结论要写清前提，否则换个环境就不成立。

## 示例

```sql
UPDATE checkpoint SET lsn = ? WHERE task = ?;
```

## 易错点

- 位点写在独立存储会产生不一致窗口

## 小结

CDC 与增量同步的位点管理 在方案对比与选型这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
