# 前端渲染阻塞与关键路径：方案对比与选型

**日期**：2026-08-26　**领域**：Web　**编号**：50/5000

## 背景

把候选方案的代价列清楚，选型就不再靠偏好。这里围绕「前端渲染阻塞与关键路径」做一次整理，重点放在能直接落地的部分。

## 要点

- 同步脚本会阻塞解析，defer 与 module 在解析后按序执行
- 关键 CSS 内联可显著改善首屏
- 选型结论要写清前提，否则换个环境就不成立。

## 示例

```js
<script src="app.js" defer></script>
```

## 易错点

- async 脚本执行顺序不可控，存在依赖时必须用 defer

## 小结

前端渲染阻塞与关键路径 在方案对比与选型这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
