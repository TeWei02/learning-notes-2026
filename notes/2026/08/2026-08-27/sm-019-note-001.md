# 树状数组与前缀和：与相邻技术的配合

**日期**：2026-08-27　**领域**：算法　**编号**：19/9000

## 背景

它很少单独出现，和上下游的约定决定了整体效果。这里围绕「树状数组与前缀和」做一次整理，重点放在能直接落地的部分。

## 要点

- 单点更新 + 区间查询，lowbit 定位父节点
- 区间和需用两个前缀和相减
- 跨组件问题先在边界处抓包或打日志。

## 示例

```python
def add(i, v):
    while i <= n:
        bit[i] += v
        i += i & -i
```

## 易错点

- 下标从 1 开始，原数组需要整体偏移

## 小结

树状数组与前缀和 在与相邻技术的配合这一面，结论可以归纳为：把前提写清楚、把失败路径覆盖到，剩下的就是按数据调优。
